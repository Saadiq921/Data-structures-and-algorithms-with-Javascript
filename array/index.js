//creates a new array
const array = new Array();
//Adds a new element to the end of an array
array.push(1);    
//Adds a new element to the beginning of an array
array.unshift(0);
// Removes an element from a beginning of an array
array.shift();
// Removes an element from a end of an array
array.pop();
//Invokes a callback for each element
array.forEach(each => console.log(each));
//size 
array.length;

